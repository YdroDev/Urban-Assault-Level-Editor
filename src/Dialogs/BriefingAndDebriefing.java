package Dialogs;

import main.MainWindow;

public class BriefingAndDebriefing {
    private final MainWindow window;

    BriefingAndDebriefing(MainWindow window) {
        this.window = window;
    }
}
